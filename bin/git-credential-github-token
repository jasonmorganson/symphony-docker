#!/bin/sh
set -eu

case "${1:-}" in
  get)
    : "${GITHUB_TOKEN:?GITHUB_TOKEN is required for private GitHub repositories}"
    printf 'protocol=https\nhost=github.com\nusername=x-access-token\npassword=%s\n' "$GITHUB_TOKEN"
    ;;
  store|erase) ;;
esac
